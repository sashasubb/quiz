<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quiz">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.ResultActivity"
            android:theme="@style/ResultActivityStyle"></activity>
        <activity android:name=".feature.GameActivity"
            android:theme="@style/GameActivityStyle"/>
        <activity
            android:name=".feature.MainActivity"
            android:theme="@style/MainActivityStyle" />
        <activity
            android:name=".feature.LoadingActivity"
            android:theme="@style/LoadingActivityStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>